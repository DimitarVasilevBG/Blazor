version: '3.8'

services:
  mongodb:
    image: mongo
    container_name: Clothy_mongodb
    ports:
      - "27017:27017"
      
  redis:
    image: redis
    container_name: Clothy_redis
    ports:
      - "6379:6379"